# Server-only secrets. NEVER prefix these with VITE_ and never commit the real values.
GEMINI_API_KEY=your_gemini_api_key
REST_COUNTRIES_API_KEY=your_rest_countries_api_key

# Optional server port
PORT=8787
